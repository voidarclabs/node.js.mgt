/**
 * -------------------------------------------------------------------------------------------
 * Copyright (c) Microsoft Corporation.  All Rights Reserved.  Licensed under the MIT License.
 * See License in the project root for license information.
 * -------------------------------------------------------------------------------------------
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { screen } from 'testing-library__dom';
import { fixture } from '@open-wc/testing-helpers';
import './mgt-spinner';
let spinner;
describe('mgt-spinner tests', () => {
    beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
        spinner = yield fixture('<mgt-spinner></mgt-spinner>');
    }));
    it('should render', () => __awaiter(void 0, void 0, void 0, function* () {
        const spinnerHtml = yield screen.findByTitle('spinner');
        expect(spinnerHtml).not.toBeNull();
        expect(spinner.shadowRoot.innerHTML).toContain('<fluent-progress-ring title="spinner"></fluent-progress-ring>');
    }));
});
//# sourceMappingURL=mgt-spinner.tests.js.map